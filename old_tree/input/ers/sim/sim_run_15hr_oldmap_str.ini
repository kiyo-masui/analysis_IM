fieldname = '15hr'

# can be either nostr, str, or ideal
sg_pwrspec_scenario = 'str'

# can be either oldmap or optimalmap
gridding = 'oldmap'

# existing map on which to base the simulations
sg_template_key = 'GBT_%s_map_oldcal:A;clean_map' % fieldname

# weight matrix to use for the mean subtraction
sg_weight_key = 'GBT_%s_map_cleaned_combined:weight;0modes' % fieldname

sg_omega_HI = 1.e-3

#-----------------------------------------------------------------------------
# automatically generated; no need to touch
treatment = '%s_%s' % (gridding, sg_pwrspec_scenario)
sg_output_root = 'sim_%s_%s_path' % (fieldname, treatment)
sg_sim_physical_key = 'sim_%s_%s_physical' % (fieldname, treatment)
sg_sim_key = 'sim_%s_%s_temperature' % (fieldname, treatment)
sg_sim_beam_key = 'sim_%s_%s_beam' % (fieldname, treatment)
sg_sim_beam_plus_data_key = 'sim_%s_%s_beam_plus_data' % (fieldname, treatment)
sg_sim_beam_plus_fg_key = 'sim_%s_%s_beam_plus_fg' % (fieldname, treatment)
sg_sim_delta_key = 'sim_%s_%s_delta' % (fieldname, treatment)
sg_sim_beam_meansub_key = 'sim_%s_%s_beam_meansub' % (fieldname, treatment)
sg_sim_beam_conv_key = 'sim_%s_%s_beam_conv' % (fieldname, treatment)
sg_sim_beam_meansubconv_key = 'sim_%s_%s_beam_meansubconv' % (fieldname, treatment)

